/* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    transition: background-color 0.5s, color 0.5s;
    background: linear-gradient(135deg, #402730, #144967); /* Gradient background */
    background-size: cover;
    background-attachment: fixed; /* Fix the background position */

}

/* Dark Theme Styles */
body.dark-theme {
    color: #fff;
}

body.dark-theme .dark-theme-text-black {
    color: #000;
}

body.dark-theme header {
    background-color: #000;
    color: white;
    border-bottom: 2px solid #3e838c;
}

body.dark-theme .header-right ul li a {
    color: white;
}

body.dark-theme .header-right ul li a:hover {
    color: #1E90FF;
}

body.dark-theme .section {
    background-color: #000;
    color: #fff;
}

body.dark-theme .contact-form {
    background: #333;
}

body.dark-theme .card {
    background-color: #333;
}

/* Light Theme Styles */
body.light-theme {
    background-color: #e0dfdf;
    color: #000;
}

body.light-theme header {
    background-color: #f1f1f1;
    color: black;
    border-bottom: 2px solid #3e838c;
}

body.light-theme .header-right ul li a {
    color: black;
}

body.light-theme .header-right ul li a:hover {
    color: #1E90FF;
}

body.light-theme .section {
    background-color: #e0dfdf;
    color: #000;
}

body.light-theme .contact-form {
    background: #e0dfdf;
}

body.light-theme .card {
    background-color: #e0dfdf;
}

/* Header Styles */
header {
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: background-color 0.5s, color 0.5s;
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 1000;
    height: 80px;
    box-sizing: border-box;
}

/* Header Left Section */
.header-left {
    display: flex;
    align-items: center;
}

.header-left img.profile-pic {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    margin-right: 1rem;
}

.header-left h1 {
    margin: 0;
    font-size: 1.5rem;
}

/* Header Right Section */
.header-right {
    display: flex;
    align-items: center;
    flex-grow: 1;
    justify-content: center;
}

.header-right ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    justify-content: flex-start;
    gap: 2rem;}

.header-right ul li {
    margin: 0;
}

.header-right ul li a {
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: bold;
    transition: color 0.5s;
}

/* Theme Toggle Button Styles */
.theme-toggle-container {
    position: absolute;
    top: 50%;
    right: 1rem;
    transform: translateY(-50%);
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: all;
}

.theme {
    display: flex;
    align-items: center;
    -webkit-tap-highlight-color: transparent;
}

.theme__fill,
.theme__icon {
    transition: 0.5s;
}

.theme__fill {
    background-color: var(--bg);
    display: block;
    mix-blend-mode: difference;
    position: fixed;
    inset: 0;
    height: 100%;
    transform: translateX(-100%);
}

.theme__icon,
.theme__toggle {
    z-index: 1;
}

.theme__icon,
.theme__icon-part {
    position: absolute;
}

.theme__icon {
    display: block;
    top: 2%;
    left: -15%;
    width: 1em;
    height: 1em;
}

.theme__icon-part {
    border-radius: 50%;
    box-shadow: 0.4em -0.4em 0 0.5em hsl(0,0%,100%) inset;
    top: calc(150% - 0.52em);
    left: calc(211.5% - 0.51em);
    width: 0.8em;
    height: 0.8em;
    transform: translate(-50%, -50%) scale(0.4);
    transition: box-shadow var(--transDur) ease-in-out,
      opacity var(--transDur) ease-in-out,
      transform var(--transDur) ease-in-out;
}

.theme__icon-part ~ .theme__icon-part {
    background-color: hsl(0,0%,100%);
    border-radius: 0.05em;
    top: 105%;
    left: 160%;
    transform: translate(-50%, -50%) rotate(0deg) translateY(0.3em);
    transform-origin: 50% 0;
    width: 0.08em;
    height: 0.16em;
}

.theme__icon-part:nth-child(3) {
    transform: translate(-50%, -50%) rotate(45deg) translateY(0.27em);
}

.theme__icon-part:nth-child(4) {
    transform: translate(-50%, -50%) rotate(90deg) translateY(0.27em);
}

.theme__icon-part:nth-child(5) {
    transform: translate(-50%, -50%) rotate(135deg) translateY(0.27em);
}

.theme__icon-part:nth-child(6) {
    transform: translate(-50%, -50%) rotate(180deg) translateY(0.27em);
}

.theme__icon-part:nth-child(7) {
    transform: translate(-50%, -50%) rotate(225deg) translateY(0.27em);
}

.theme__icon-part:nth-child(8) {
    transform: translate(-50%, -50%) rotate(270deg) translateY(0.3em);
}

.theme__icon-part:nth-child(9) {
    transform: translate(-50%, -50%) rotate(315deg) translateY(0.3em);
}

.theme__label,
.theme__toggle,
.theme__toggle-wrap {
    position: relative;
}

.theme__toggle,
.theme__toggle:before {
    display: block;
}

.theme__toggle {
    background-color: hsl(48,90%,85%);
    border-radius: 25% / 50%;
    box-shadow: 0 0 0 0.1em var(--primaryT);
    padding: 0.2em;
    width: 4em;
    height: 2em;
    -webkit-appearance: none;
    appearance: none;
    transition: background-color var(--transDur) ease-in-out,
      box-shadow 0.15s ease-in-out,
      transform var(--transDur) ease-in-out;
}

.theme__toggle:before {
    background-color: hsl(48,90%,55%);
    border-radius: 50%;
    content: "";
    width: 1.6em;
    height: 1.6em;
    transition: 0.5s;
}

.theme__toggle:focus {
    box-shadow: 0 0 0 0.1em var(--primary);
    outline: transparent;
}

.theme__toggle:checked {
    background-color: hsl(198,90%,15%);
}

.theme__toggle:checked:before,
.theme__toggle:checked ~ .theme__icon {
    transform: translateX(2em);
}

.theme__toggle:checked:before {
    background-color: hsl(198,90%,55%);
}

.theme__toggle:checked ~ .theme__fill {
    transform: translateX(0);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(1) {
    box-shadow: 0.2em -0.2em 0 0.2em hsl(0,0%,100%) inset;
    transform: scale(1);
    top: 0.59em;
    left: 1em;
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part ~ .theme__icon-part {
    opacity: 0;
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(2) {
    transform: rotate(45deg) translateY(0.8em);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(3) {
    transform: rotate(90deg) translateY(0.8em);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(4) {
    transform: rotate(135deg) translateY(0.8em);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(5) {
    transform: rotate(180deg) translateY(0.8em);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(6) {
    transform: rotate(225deg) translateY(0.8em);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(7) {
    transform: rotate(270deg) translateY(0.8em);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(8) {
    transform: rotate(315deg) translateY(0.8em);
}

.theme__toggle:checked ~ .theme__icon .theme__icon-part:nth-child(9) {
    transform: rotate(360deg) translateY(0.8em);
}

.theme__toggle-wrap {
    margin: 0 0.75em;
}

@supports selector(:focus-visible) {
    .theme__toggle:focus {
        box-shadow: 0 0 0 0.1em var(--primaryT);
    }

    .theme__toggle:focus-visible {
        box-shadow: 0 0 0 0.1em var(--primary);
    }
}

/* Home Section Styles */
.home-section {
    position: relative;
    height: 100vh;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    color: #fff;
    overflow: hidden;
    background-attachment: fixed;
}

.home-section::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.25);
    z-index: 1;
}

.home-content {
    position: relative;
    z-index: 2;
}

.home-image {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    margin-bottom: 1rem;
}

.home-text {
    font-size: 2rem;
    font-weight: bold;
}

/* About Section Styles */
.section {
    padding: 4rem 1.5rem;
}

.is-primary {
    background-color: #0999ff;
    color: white;
}

.has-text-centered {
    text-align: center;
}

.container {
    max-width: 1600px;
    margin: 0 auto;
}

.columns {
    display: flex;
    justify-content: center;
}

.column {
    flex: 1;
    max-width: 100%;
}

.is-three-fifths {
    flex: 0 0 60%;
}

/* About Card Styles */
.about-card {
    width: 100%;
    max-width: 1200px;
    height: auto;
    margin: auto;
    background-color: #4158D0;
    background-image: linear-gradient(43deg, #195e63 0%, #063940 50%, #195e63 100%);
    border-radius: 8px;
    color: white;
    overflow: hidden;
    position: relative;
    transform-style: preserve-3d;
    perspective: 1000px;
    transition: all 0.3s ease-in-out;
    cursor: pointer;
}

.about-card-content {
    padding: 20px;
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: 10px;
    color: white;
    align-items: center;
    justify-content: center;
    text-align: center;
}

.about-card-content .card-title {
    font-size: 28px;
    font-weight: 700;
    color: inherit;
}

.about-card-content .card-para {
    color: inherit;
    font-size: 20px;
    line-height: 1.5;
    text-align: center; /* Ensure text is centered */
}

/* Adjusting the card's behavior on hover */
.about-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 30px rgba(0, 0, 0, 0.2);
}

.about-card:before,
.about-card:after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.1));
    z-index: 1;
}

.about-card:before {
    transform: translateX(-100%);
    transition: transform 0.5s ease-in-out;
}

.about-card:after {
    transform: translateX(100%);
    transition: transform 0.5s ease-in-out;
}

.about-card:hover:before,
.about-card:hover:after {
    transform: translateX(0);
}

/* Skills Section Styles */
.skills {
    background-image: url('../images/fondo_tabla_negro.png');
    background-size: cover;
    padding: 4rem 1.5rem;
}

body.dark-theme .skills {
    background-image: url('images/fondo_tabla_negro.png');
}

body.light-theme .skills {
    background-image: url('images/fondo_tabla_blanco.png');
}

.is-narrow {
    max-width: 1400px;
}

.box {
    background-color: white;
    padding: 2rem;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2), 0 0 0 1px rgba(10, 10, 10, 0.1);
    border-radius: 12px;
}

.content {
    margin: 0;
}

.icon-stack {
    position: relative;
    display: inline-block;
    width: 4rem;
    height: 4rem;
    line-height: 4rem;
    vertical-align: middle;
}

.icon-2x {
    font-size: 2em;
}

.icon-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 50%;
}

.icon-background {
    background-color: #195e63;
    border-radius: 50%;
    padding: 0.5rem; 
    display: inline-block; 
}

.skill-container {
    padding: 2rem;
    text-align: center;
}

.title {
    margin-bottom: 1.5rem;
    font-size: 1.5rem;
    font-weight: 600;
}

.list-title {
    margin-top: 3.5rem;
    margin-bottom: 0.5rem;
}

.has-text-primary {
    color: #195e63;
}

.has-text-weight-normal {
    font-weight: normal;
}

ul {
    list-style-type: none;
    padding: 0;
}

ul li {
    margin-bottom: 0.5rem;
}

.column {
    border-left: 1px solid #e0e0e0;
}

.column:first-child {
    border-left: none;
}

/* Proyectos Section Styles */
#projects {
    padding: 4rem 1.5rem;
    text-align: center;
}

.projects-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 2rem; /* Espacio entre las tarjetas */
}

#projects .card {
    background-color: #333; /* Fondo de las tarjetas en gris oscuro para destacar sobre el negro */
    color: #fff; /* Color del texto en blanco */
}

#projects .card .heading {
    color: #e81cff; /* Asegura que el texto del heading sea visible */
}

.card {
    position: relative;
    width: 250px;
    height: 330px;
    display: flex;
    flex-direction: column;
    justify-content: end;
    padding: 16px;
    gap: 16px;
    border-radius: 12px;
    cursor: pointer;
    background-color: #333; /* Fondo de las tarjetas en gris oscuro para destacar sobre el negro */
    color: #fff; /* Color del texto en blanco */
    border: 2px solid transparent; /* Inicialmente transparente para permitir el efecto de hover */
    transition: border-color 0.3s ease; /* Transición para el borde */
}

.card::before {
    content: '';
    position: absolute;
    inset: 0;
    left: -7px;
    margin: auto;
    width: 295px;
    height: 375px;
    border-radius: 14px;
    background: linear-gradient(-45deg, #e81cff 0%, #40c9ff 100%);
    z-index: -10;
    pointer-events: none;
    transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.card::after {
    content: "";
    z-index: -1;
    position: absolute;
    inset: 0;
    background: linear-gradient(-45deg, #fc00ff 0%, #00dbde 100%);
    transform: translate3d(0, 0, 0) scale(0.95);
    filter: blur(20px);
}

.heading {
    font-size: 24px;
    text-transform: capitalize;
    font-weight: 700;
}

.card p:not(.heading) {
    font-size: 18px;
}

.card p:last-child {
    color: #e81cff;
    font-weight: 600;
}

.card:hover {
    border-color: #e81cff; /* Color del borde al hacer hover */
}

.card:hover::after {
    filter: blur(30px);
}

.card:hover::before {
    transform: rotate(-180deg);
}
/* Contact Section Styles */
.contact-section {
    padding: 4rem 1.5rem;
    text-align: center;
    background-size: cover; /* Ensure background image covers the section */
    background-position: center; /* Center the background image */
    background-repeat: no-repeat; /* Do not repeat the background image */
    background-attachment: fixed; /* Fix the background image position */
    color: #000; /* Dark text for light theme */
}

.contact-section .container {
    width: 600px; /* Narrower form */
    margin: 0 auto;
}

.section-title {
    font-size: 2.5rem;
    font-weight: bold;
    margin-bottom: 1rem;
}

.section-subtitle {
    font-size: 1.2rem;
    margin-bottom: 2rem;
}

.contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 2rem;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    max-width: 600px; /* Ensure form width is capped */
    margin: 0 auto; /* Center the form */
}

.form-group {
    display: flex;
    flex-direction: column;
    text-align: left;
}

.contact-form label {
    margin-bottom: 0.5rem;
    font-weight: bold;
    font-size: 1rem;
    color: #000; /* Label text color */
}

.contact-form input,
.contact-form textarea {
    padding: 1rem;
    border: 1px solid #ccc;
    border-radius: 4px;
    background-color: #f0f4f8; /* Input background color */
    font-size: 1rem;
    color: #333; /* Input text color */
}

.contact-form textarea {
    height: 200px; /* Increased height for message textarea */
}

.contact-form input::placeholder,
.contact-form textarea::placeholder {
    color: #000;
}

.submit-button {
    padding: 1rem;
    border: none;
    border-radius: 4px;
    background-color: #333; /* Button background color */
    color: #fff; /* Button text color */
    font-size: 1.2rem;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.submit-button:hover {
    background-color: #555;
}

/* Adjust styles for dark theme */
body.dark-theme .contact-section {
    color: #fff; /* Light text for dark theme */
}

body.dark-theme .contact-form label {
    color: #fff; /* Form text color */
}

body.dark-theme .contact-form {
    background:#444; /* Form background color */
    color: #fff; /* Form text color */
}

body.dark-theme .contact-form input,
body.dark-theme .contact-form textarea {
    background-color: #b3b3b3; /* Input background color for dark theme */
    color: #000; /* Input text color for dark theme */
}

body.dark-theme .contact-form input::placeholder,
body.dark-theme .contact-form textarea::placeholder {
    color: #000;
}

body.dark-theme .submit-button {
    background-color: #fff; /* Button background color for dark theme */
    color: #000; /* Button text color for dark theme */
}

body.dark-theme .submit-button:hover {
    background-color: #bbb;
}
